{
  "name": "@eye-of-atlas/api-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker key broker and provider-compliance layer for Eye of Atlas",
  "license": "MIT",
  "engines": { "node": ">=24.14.0" },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test \"test/*.test.mjs\"",
    "lint": "node --check src/index.js && node --check src/lib/store.js && node --check src/lib/json.js && node --check src/middleware/origin.js && node --check src/middleware/ratelimit.js && node --check src/middleware/budget.js && node --check src/middleware/cache.js && node --check src/routes/ping.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
